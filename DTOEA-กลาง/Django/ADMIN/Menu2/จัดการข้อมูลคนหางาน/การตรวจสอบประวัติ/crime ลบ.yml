info:
  name: crime ลบ
  type: http
  seq: 4

http:
  method: PUT
  url: http://apiserver:8080/admin/crime/1/
  body:
    type: json
    data: "{

         "action" : "deleteCrimeInfo"
      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
