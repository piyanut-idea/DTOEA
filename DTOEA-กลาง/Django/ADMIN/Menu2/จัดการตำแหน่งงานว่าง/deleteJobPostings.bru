meta {
  name: deleteJobPostings
  type: http
  seq: 6
}

put {
  url: http://apiserver:8080/admin/jobpostings/4/
  body: json
  auth: inherit
}

body:json {
  // ใช้ในกรณีลบ jobPostings ในกรณีที่ถูก submit แล้ว (มี records อยู่ใน jobPostingsInfo)
  {
      "action": "deleteJobPostingsInfo"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
