meta {
  name: deletePositionPostings
  type: http
  seq: 5
}

put {
  url: http://apiserver:8080/admin/positionpostings/2/
  body: json
  auth: inherit
}

body:json {
  // ใช้ในกรณีลบ positionPostings ในกรณีที่ถูก submit แล้ว (มี records อยู่ใน positionPostingsInfo)
  {
      "action": "deletePositionPostingsInfo"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
