meta {
  name: Admin-SendSeekerList-cancel
  type: http
  seq: 22
}

put {
  url: http://apiserver:8080/admin/sendseeker/1/
  body: json
  auth: inherit
}

body:json {
  {
      // ยกเลิกการสมัครสมาชิกกองทุน (krsendsk-seekerlist)
      "actiontype":"cancel"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
