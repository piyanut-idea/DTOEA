meta {
  name: บันทึก Step 3
  type: http
  seq: 4
}

put {
  url: http://apiserver:8080/web/recruit/9/
  body: json
  auth: inherit
}

body:json {
  {
      "actiontype":"recruit-step3"
    // gen reqcode, tmpcode
    // set reqstatus=1, reqdate=timezone.now() 
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
