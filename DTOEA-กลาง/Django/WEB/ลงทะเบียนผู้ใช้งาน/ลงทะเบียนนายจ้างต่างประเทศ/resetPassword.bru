meta {
  name: resetPassword
  type: http
  seq: 15
}

put {
  url: http://apiserver:8080/web/interdtl/id/
  body: json
  auth: inherit
}

body:json {
  {
    "action": "resetPassword",
    "password": "newpassword",
    "confirm_password": "newpassword"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
